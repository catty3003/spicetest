<%= form_for(@matrixgenu) do |f| %>
  <% if @matrixgenu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@matrixgenu.errors.count, "error") %> prohibited this matrixgenu from being saved:</h2>

      <ul>
      <% @matrixgenu.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :m_genus %><br>
    <%= f.text_field :m_genus %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@matrixproductform) do |f| %>
  <% if @matrixproductform.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@matrixproductform.errors.count, "error") %> prohibited this matrixproductform from being saved:</h2>

      <ul>
      <% @matrixproductform.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_form %><br>
    <%= f.text_field :product_form %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
